# AI_Timeline_Builder 运行依赖
#
# 安装：python -m pip install -r requirements.txt
#
# PyQt5      GUI
# jsonschema Timeline JSON 的 Schema 校验。缺它时 core/timeline_validator.py
#            的 _validate_schema() 会静默跳过（HAS_JSONSCHEMA=False），
#            只剩语义规则在跑，很容易误以为「0 问题」＝完全合规。
# pytest     测试（tests/）
PyQt5==5.15.11
jsonschema==4.23.0
pytest==8.3.4

# 可选：Edge 神经网络语音（core/voice.py 的 EdgeVoiceProvider）
# 不装也能跑 —— 配音会退回 Windows 自带 SAPI（只有一个英文女声，听感机械）。
# 装了并且能联网时 core.voice.best_provider() 会优先选它。
edge-tts==7.2.8
